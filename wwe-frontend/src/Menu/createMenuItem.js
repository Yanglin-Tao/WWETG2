import React from 'react';
/* TODO: This component should provide a form to allow dining hall administrator create a new menuItem. 
    A newMenuItem should have a name, a category, a list of ingredients, a list of dietary tags, calories per serving, 
    and a rating intialized to null.
 */
function CreateMenuItem() {
    return (
        <div>
            CreateMenuItem
        </div>
    );

}

export default CreateMenuItem;